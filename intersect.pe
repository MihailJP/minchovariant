#!/usr/bin/env fontforge

if ( $argc < 3 )
	PostNotice(StrJoin(["Usage:", $0, "infont outfont"], " "))
	Quit(1)
endif

Open($1)
SelectWorthOutputting()
foreach
	RoundToInt()
	FindIntersections()
endloop
Save($2)
